<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/runTrackMap">

    <action
        android:id="@+id/action_global_trackingFragment"
        app:destination="@+id/runTrackMap"
        app:launchSingleTop="true"/>
    <fragment
        android:id="@+id/runTrackMap"
        android:name="com.example.runningtracking.RunTrackMap"
        android:label="fragment_run_track_map"
        tools:layout="@layout/fragment_run_track_map" />
</navigation>